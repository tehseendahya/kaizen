import LandingClient from './LandingClient';

export const metadata = {
  title: 'Axis — Your personalized learning platform',
  description: 'AI-powered tools, course insights, and a community of learners.',
};

export default function Page() {
  return <LandingClient />;
}
